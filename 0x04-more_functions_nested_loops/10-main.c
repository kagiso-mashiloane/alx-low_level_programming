#include "main.h"

/**
 * main - check the code
 * Description: If size is 0 or less, the function should print only a new line
 * Return: Always 0.
 */
int main(void)
{
	print_triangle(2);
	print_triangle(10);
	print_triangle(1);
	print_triangle(0);
	return (0);
}
